	jmp main

# t
# b
# a
# --
# a + (b - a) * t
lerp:
	dup 3
	dup 3
	dup 1

	subf
	dup 3
	mulf
	addf

	# Clean up the stack
	swap 2
	pop
	swap 2
	pop
	swap 2
	pop

	ret

main:
	push 69.0
	push 420.0
	push 0.0

	call lerp
	native 2

	push 69.0
	push 420.0
	push 1.0

	call lerp
	native 2


	push 69.0
	push 420.0
	push 0.5

	call lerp
	native 2

	halt
